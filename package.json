{
  "name": "katalogizimi",
  "private": true,
  "license": "GPL-2.0",
  "description": "Root scripts delegate to the Next.js app in site/",
  "scripts": {
    "dev": "npm run dev --prefix site",
    "build": "npm run build --prefix site",
    "start": "npm run start --prefix site",
    "lint": "npm run lint --prefix site",
    "seed:catalog": "npm run seed:catalog --prefix site"
  }
}
