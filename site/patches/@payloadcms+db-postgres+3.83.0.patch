diff --git a/node_modules/@payloadcms/db-postgres/dist/connect.js b/node_modules/@payloadcms/db-postgres/dist/connect.js
index bf4beed..674f371 100644
--- a/node_modules/@payloadcms/db-postgres/dist/connect.js
+++ b/node_modules/@payloadcms/db-postgres/dist/connect.js
@@ -35,6 +35,9 @@ const connectWithReconnect = async function({ adapter, pool, reconnect = false }
         // swallow error
         }
     });
+    // Return listener client to the pool. Upstream leaves it checked out forever, which forces
+    // pool.max >= 2 and can exhaust Supabase session pooler slots (EMAXCONNSESSION / pool_size: 15).
+    result.release();
 };
 export const connect = async function connect(options = {
     hotReload: false
