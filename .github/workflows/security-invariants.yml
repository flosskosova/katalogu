name: Security invariants

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Reject a commit that drops a shipped security control
        run: node site/scripts/check-security-invariants.mjs
